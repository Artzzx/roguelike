[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
tcod = ">=13.10.0"
numpy = ">=1.24.0"
colorama = ">=0.4.6"
esper = ">=2.5"
pyyaml = ">=6.0"
jsonpickle = ">=3.0.0"
pygame = ">=2.1.0"
bearlibterminal = ">=0.15.7"
pytest = ">=7.3.1"
black = ">=23.3.0"
flake8 = ">=6.0.0"
mypy = ">=1.3.0"
sphinx = ">=7.0.0"

[dev-packages]

[requires]
python_version = "3.11"
